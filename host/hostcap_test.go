package host_test

import (
	"testing"
)

func TestCapture(t *testing.T) {
	// msg := host.Capture()
	// assert.Equal(t, message.Create(message.OpEcho, 1, []byte{}), msg)
}

func TestCaptureRid(t *testing.T) {
	// msg := host.CaptureRid("init")
	// assert.Equal(t, "ok", msg.Name)
	// assert.Equal(t, "init", msg.Get("rid"))
}
